/* eslint-disable no-console */
const authorize = () => {
  console.log('authorizing with Twitter');
};

const post = () => {
  console.log('posting on Twitter');
};

export {
  authorize,
  post,
};
